# Define the generic agent workflow: Perception → Reasoning → Action → Reflection.

Show how this workflow maps onto Google Cloud services (Vertex AI, BigQuery, Cloud Storage, APIs).

Include a simple diagram (ASCII or Markdown) plus links to detailed use cases.

# Use Case Pages
Each use case gets its own page with:

Problem Statement (what the agent is solving).

Architecture Diagram (text-based or schematic).

Workflow Explanation (step-by-step).

Code Snippet (minimal working example).

Interview Angle (how to explain this use case in an interview).
